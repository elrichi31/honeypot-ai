# Cowrie credential database
# Format: username:x:password
#
# Wildcard catch-all: any username with any password >= 4 chars gets in.
# The length floor filters trivial 1-3 char noise but lets real botnet
# wordlists (Admin@12345, Google@123, developer123, etc.) succeed and
# proceed to the post-auth shell where commands + malware are captured.
#
# *:x:* would accept everything including 1-char passwords — use the
# explicit wildcards below to enforce a minimal floor via patch_auth.py.

*:x:*
