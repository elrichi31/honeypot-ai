# i18n plan & debt

The dashboard has a homegrown i18n system in
[`apps/dashboard/lib/i18n/`](../../apps/dashboard/lib/i18n/) — no external lib.
Locales `en` / `es`, namespaced dictionaries under `dicts/`, a tiny `translate()`
with `{var}` interpolation and English fallback. **English is the source of
truth**; every locale must provide the same keys.

## Convention

- All user-facing strings are written in **English first**, then layered into the
  i18n dictionaries for Spanish (and any future locale).
- **Never hardcode Spanish (or any non-English) text in components or routes.**
  Add a key to the matching `dicts/*.ts` file instead.

## Done

- **2026-06-19 — Localized session classification (debt #1, partial).** Routed
  the `session-classify-v2.ts` labels + summaries through `translate()`. Pattern:
  `classify()` now returns a stable, non-translatable `key: ClassificationKey`
  (used for severity ranking in `SEVERITY_ORDER` / `worstOf`) plus a
  `summaryKey` + `summaryVars`; the visible text is translated at render time via
  `useT()`. New dict namespace
  [`lib/i18n/dicts/sessions.ts`](../../apps/dashboard/lib/i18n/dicts/sessions.ts)
  holds all 16 labels + summaries in `en`/`es`. Updated consumers:
  `components/session-row.tsx`, `ip-session-group.tsx`, `scan-group-row.tsx`,
  `sessions-table.tsx` (filter dropdown now keys on the stable `key`, shows the
  localized label). Also folded leaked Spanish badges ("Comprometido",
  "{n} sesiones", "Sin eventos registrados.") into the same namespace. Added
  `lib/session-classify-v2.test.ts` (keys, severity ranking, per-locale dict
  coverage). Verified: `tsc --noEmit` clean, `tsx --test` green.

- **2026-06-18 — Spanish cleanup.** Swept the repo for Spanish strings that had
  leaked outside the i18n dictionaries and translated them back to English in
  place. Affected files:
  - Dashboard: `app/users/page.tsx`, `lib/session-classify-v2.ts`,
    `lib/risk-score.ts`, `lib/threat-graph.ts`, `lib/botnet-signatures.ts`,
    `lib/roles-shared.ts`, `lib/ip-enrichment.ts` (Discord embed),
    and the API routes under `app/api/users/`, `app/api/sessions-admin/`,
    `app/api/alerts/test/`.
  - Ingest API: `src/modules/ingest/ingest.service.ts` (login Discord alert),
    `src/lib/weekly-report.ts` (periodic report).
  - Left in Spanish on purpose: `dicts/*` (the `es` translations), `lib/timezones.ts`
    (proper place names), and the AI prompt in
    `app/api/ai/session-summary/route.ts` (see debt below).

## Debt / TODO

1. **Move hardcoded English strings into the dictionaries.** Remaining `lib/`
   modules with user-facing English literals: `threat-graph.ts` (node labels),
   `botnet-signatures.ts` (signature names/descriptions), `roles-shared.ts` (role
   names/descriptions). Apply the same pattern proven on `session-classify-v2.ts`
   (2026-06-19): keep a stable key in the `lib/` module, translate at the render
   site with `useT()` / `getServerT()` — do **not** inject locale into the module.
   - `session-classify-v2.ts` — ✅ done 2026-06-19 (see Done above).
   - `roles-shared.ts` — ✅ done 2026-06-19. Replaced `ROLE_LABELS`/`ROLE_DESCRIPTIONS` (hardcoded English strings) with `ROLE_LABEL_KEYS`/`ROLE_DESCRIPTION_KEYS` (stable `TranslationKey` references). New dict namespace `lib/i18n/dicts/users.ts` holds 4 labels + 4 descriptions in `en`/`es`. Updated consumers: `app/users/page.tsx`, `app/profile/page.tsx`. `tsc --noEmit` clean.
   - `threat-graph.ts` + `botnet-signatures.ts` — ✅ done 2026-06-20. Added `nameKey`/`descriptionKey`/`categoryKey` to `BotnetSignature`/`BotnetMatch`. New dict namespace `dicts/threat-intel.ts` (family names/descriptions, category labels, graph node labels, `threat-intel-card` UI strings) in `en`/`es`. Updated: `threat-intel-card.tsx` (uses `useT()` for all strings incl. section titles, tooltips, aria-labels), `threat-graph-view.tsx` (renders `labelKey` when present), `threat-graph.ts` (`"Reputation"` node carries `labelKey`). `tsc --noEmit` clean.
   - `risk-score.ts` — **not actually item #1.** Its only consumer is
     `app/api/ai/session-summary/route.ts`, which feeds `breakdown[].label`,
     `level`, and `preClassify()` into the AI prompt (already Spanish). None of
     these strings reach a screen, so localizing them = making the AI prompt
     follow the locale → belongs to item #2 below, not here.

2. **AI prompt should follow the user's locale.** ✅ done 2026-06-20. `app/api/ai/session-summary/route.ts` now reads the per-user locale via `getServerLocale()` and instructs the model to respond in the active language. Prompt rewritten in English (source of truth); language injected at call time via `LOCALE_LANGUAGE` map. Hardcoded Spanish error message also removed.

3. **Server-side / Discord / report strings have no locale path.** ✅ resolved 2026-06-20. Audited `ingest.service.ts`, `threat-alerts.ts`, `threat-checks.ts`, `weekly-report.ts`, and `discord.ts` — all backend alert strings are already in English after the 2026-06-18 cleanup. Decision: backend alerts (Discord, weekly report, sensor-offline) stay English-only; the backend has no user context and English is the correct language for system-level ops alerts.

4. **Full English audit — remaining hardcoded Spanish in components.** ✅ done 2026-06-21. Grepped entire `apps/dashboard` for accented characters and Spanish UI patterns. Found 5 hardcoded Spanish strings in two components:
   - `app/web-attacks/timeline/timeline-charts.tsx` — `"Ranking de ataques"`, `"Ordenado por frecuencia · all-time"`.
   - `app/web-attacks/geo/web-geo-map.tsx` — `"Sin datos geolocalizables"`, `"Las IPs privadas/Docker no tienen geo"`, `"Ordenado por total de hits · click en una IP para ver su detalle"`, plus two Spanish table headers (`"Top amenaza"`, `"% del total"`).
   All moved to new dict namespace `lib/i18n/dicts/web-attacks.ts` (`webAttacks.*` keys, `en`/`es`). Both components now use `useT()`. `tsc --noEmit` clean.
   Remaining Spanish in codebase: code comments only (`auth-client.ts`, `ip-enrichment.ts`, `web-attacks/[ip]/page.tsx`) — not user-facing, no action needed.

5. **Second pass audit — 3 remaining Spanish strings.** ✅ done 2026-06-21. Re-audited after item 4. Found:
   - `app/users/page.tsx:385` — `"Sin asignar"` / `"Selecciona tenant"` in tenant select `<option>`. Added `users.tenant.unassigned` + `users.tenant.select` keys to `dicts/users.ts`; page already had `useT()`.
   - `components/ioc-section.tsx:110` — `"Sin indicadores de este tipo."` in empty state. Added `iocs.section.empty` key to `dicts/iocs.ts`; added `useT()` to `IocSection`.
   - `components/network/utils.ts:15` — `"Sin cliente"` as fallback group name (pure module). Pattern: added `groupKey` field to `ClientLabelData`; `rf-client-label.tsx` resolves `t("sensors.unassigned")` when `groupKey === "__unassigned__"`. `sensors.unassigned` key already existed. `tsc --noEmit` clean.
   **All user-facing Spanish strings are now gone from the dashboard. Only Spanish remaining: code comments (non-user-facing).**
