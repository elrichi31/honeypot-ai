export const dynamic = "force-dynamic"

import { ProtocolServicePage } from "../protocol-service-page"

export default function PortScanServicePage() {
  return <ProtocolServicePage protocol="port-scan" />
}
